spring.application.name=coin-bomb-backend
# Database Configuration (will be overridden by environment variables in production)
# H2 Database Configuration (for local development)
# spring.datasource.url=jdbc:h2:mem:testdb
# spring.datasource.username=sa
# spring.datasource.password=password
# spring.h2.console.enabled=true

# Generic settings that work with any database
spring.jpa.hibernate.ddl-auto=update
